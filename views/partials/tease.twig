class="tease tease-{{post.post_type}}" id="tease-{{post.ID}}"
	{% block content %}
		<h2><a href="{{post.link}}">{{post.title}}</a></h2>
		{{post.get_preview}}
		{% if post.get_thumbnail %}
			<img src="{{post.thumbnail.src}}" />
		{% endif %}
	{% endblock %}